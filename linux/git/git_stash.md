````markdown
# Шпаргалка по `git stash`

`git stash` — это способ **временно спрятать (сохранить) незакоммиченные изменения**, чтобы:

- переключиться на другую ветку,
- сделать pull/merge/rebase,
- поэкспериментировать,
- а потом вернуть свои изменения обратно.

Думай о stash как о «черновике», который живёт в отдельном стеке.

---

## 1. Базовая идея

Ты что-то менял в файлах, но:

- ещё не готов коммитить,
- нужно срочно:
  - перейти на другую ветку,
  - обновить `main`,
  - быстро поправить баг.

Решение:

```bash
git stash           # спрятать текущие изменения
# ... делаем что-то другое ...
git stash pop       # достать обратно и применить
````

---

## 2. Базовые команды

### 2.1. Спрятать изменения: `git stash`

```bash
git stash
```

По умолчанию:

- сохраняет изменения **только отслеживаемых файлов** (изменённые, staged/unstaged),
    
- очищает рабочее дерево (working directory) и индекс (staging),
    
- добавляет запись в стек stash под именем типа: `stash@{0}`.
    

Можно дать описание:

```bash
git stash save "фикс верстки хедера"
# или в новых версиях:
git stash push -m "фикс верстки хедера"
```

---

### 2.2. Просмотреть список stash’ей

```bash
git stash list
```

Пример:

```text
stash@{0}: On main: фикс верстки хедера
stash@{1}: On feature/login: WIP: login form
stash@{2}: On main: эксперимент с логированием
```

---

### 2.3. Применить и удалить: `git stash pop`

```bash
git stash pop
```

- Берёт **последний** stash (`stash@{0}`),
    
- применяет изменения к текущему рабочему дереву,
    
- удаляет этот stash из списка.
    

Применить **конкретный**:

```bash
git stash pop stash@{2}
```

> ⚠️ При применении могут возникнуть конфликты — их нужно решить как при merge, затем `git add` и коммит.

---

### 2.4. Применить, но не удалять: `git stash apply`

```bash
git stash apply
# или конкретный:
git stash apply stash@{1}
```

- Применяет изменения,
    
- **stash остаётся** в списке.
    

Полезно, если хочешь применить один и тот же stash к нескольким веткам или «на всякий случай» оставить резервную копию.

---

### 2.5. Удалить stash: `git stash drop` и `clear`

Удалить конкретный:

```bash
git stash drop stash@{1}
```

Удалить **все**:

```bash
git stash clear
```

---

## 3. Что именно попадает в stash

### 3.1. По умолчанию — только отслеживаемые файлы

```bash
git stash
```

Сохраняет:

- изменения в уже отслеживаемых файлах (modified, staged/unstaged),
    
- но **не трогает**:
    
    - новые файлы (untracked),
        
    - игнорируемые файлы.
        

### 3.2. Включить новые (untracked) файлы: `-u`

```bash
git stash -u
# то же:
git stash push -u -m "сохранил всё вместе с новыми файлами"
```

Сохраняет:

- отслеживаемые изменения,
    
- **новые** файлы (untracked),
    
- но **не игнорируемые**.
    

### 3.3. Включить вообще всё (даже игнорируемое): `-a`

```bash
git stash -a
# или:
git stash push -a -m "полный снепшот"
```

Сохраняет:

- отслеживаемые,
    
- новые (untracked),
    
- игнорируемые файлы.
    

---

## 4. Частичный stash (по кускам / по файлам)

### 4.1. По файлам

Можно указать путь(и):

```bash
git stash push src/app.py templates/base.html
```

Спрятает только изменения в этих файлах.

### 4.2. По кускам (`-p`, patch mode)

```bash
git stash -p
# или:
git stash push -p
```

Git покажет изменения по hunks и спросит, какие куски отправить в stash, а какие оставить.

---

## 5. Смотреть, что внутри stash

### 5.1. Кратко: `git stash list`

Уже было:

```bash
git stash list
```

### 5.2. Посмотреть diff: `git stash show`

```bash
git stash show
```

- Показывает краткое резюме (какие файлы поменялись).
    

С полным диффом:

```bash
git stash show -p
# или для конкретного:
git stash show -p stash@{2}
```

---

## 6. Создать ветку из stash: `git stash branch`

Очень полезная штука, если:

- ты долго экспериментировал,
    
- наделал кучу изменений в одной ветке,
    
- а теперь хочешь вынести это в отдельную feature-ветку.
    

```bash
git stash branch new-feature-branch
```

Git сделает:

1. Создаст ветку `new-feature-branch` от того коммита, где stash был сделан.
    
2. Переключится на неё.
    
3. Попробует применить stash.
    
4. Если успех — удалит этот stash из списка.
    

Можно указать конкретный stash:

```bash
git stash branch new-idea stash@{1}
```

---

## 7. Типичные сценарии использования

### 🟢 Сценарий 1: нужно срочно переключиться на другую ветку

```bash
# ты на feature/login, есть незакоммиченные изменения
git stash           # спрятал
git checkout main   # или git switch main
# сделал hotfix / pull / merge ...
git checkout feature/login
git stash pop       # вернул изменения
```

---

### 🟢 Сценарий 2: есть новые файлы, тоже нужно спрятать

```bash
git stash -u        # спрятать и tracked, и untracked
# работаешь...
git stash pop
```

---

### 🟢 Сценарий 3: хочешь превратить текущую WIP-работу в отдельную ветку

```bash
git stash save "эксперименты с формой"
git stash branch feature/experiments
# теперь ты в новой ветке, изменения применены, stash удален
```

---

### 🟢 Сценарий 4: «почти всё ок, но нужна копия на всякий случай»

```bash
git stash
git stash apply     # применяем, но stash остаётся
# если всё хорошо и stash больше не нужен:
git stash drop stash@{0}
```

---

## 8. Возможные проблемы и нюансы

1. **Конфликты при `stash pop/apply`**
    
    - Такие же, как при merge.
        
    - Решение:
        
        - правишь конфликтные файлы,
            
        - `git add ...`,
            
        - при необходимости коммитишь.
            
2. **Используешь `stash` как «мусорное ведро навсегда»**
    
    - Со временем сложно понять, что где.
        
    - Лучше:
        
        - давать внятные сообщения (`-m`),
            
        - периодически чистить (`git stash clear`),
            
        - или превращать в ветку (`git stash branch`).
            
3. **Забыл, что там было**
    
    - Есть `git stash show -p stash@{N}`, чтобы посмотреть содержимое подробно и решить, нужно ли это.
        

---

## 9. Краткая таблица по `git stash`

|Задача|Команда|
|---|---|
|Спрятать изменения (tracked)|`git stash`|
|Спрятать с описанием|`git stash push -m "message"`|
|Спрятать + новые файлы (untracked)|`git stash -u`|
|Спрятать вообще всё (даже игнорируемое)|`git stash -a`|
|Спрятать изменения в конкретных файлах|`git stash push file1 file2`|
|Спрятать только часть изменений (patch)|`git stash -p`|
|Посмотреть список stash’ей|`git stash list`|
|Посмотреть, что внутри stash|`git stash show -p stash@{N}`|
|Применить и удалить последний stash|`git stash pop`|
|Применить конкретный stash и удалить|`git stash pop stash@{N}`|
|Применить, но оставить stash|`git stash apply`|
|Удалить конкретный stash|`git stash drop stash@{N}`|
|Очистить все stash’и|`git stash clear`|
|Создать ветку из stash|`git stash branch new-branch [stash@{N}]`|

---

```

Если хочешь, могу сделать мини-сценарий именно под твой текущий рабочий процесс: например, как правильно стэшить изменения в Django-проекте перед `git pull`/миграциями и потом аккуратно их возвращать.
::contentReference[oaicite:0]{index=0}
```