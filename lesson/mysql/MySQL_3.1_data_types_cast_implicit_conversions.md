# MySQL — Урок 3.1. Типы данных. Преобразование типов (CAST). Скрытые преобразования

## 0) Контекст урока
Работаем с БД **northwind** (доступ только на чтение). fileciteturn15file1

---

## 1) Зачем нужны типы данных
**Тип данных** — это “категория” значения, которая определяет, *что можно хранить в столбце* и *какие операции можно делать* с этими данными. fileciteturn15file0turn15file1

Причины использовать правильные типы: fileciteturn15file0turn15file1
- **Правильное хранение**: числа/даты/текст хранятся по-разному.
- **Эффективность**: корректные типы ускоряют операции (сравнения, вычисления).
- **Меньше ошибок**: нельзя случайно записать текст туда, где должны быть числа.
- **Корректные операции**: текст можно склеивать, числа — складывать и т.д.

---

## 2) Основные типы данных (что нужно помнить)
### 2.1. Числовые fileciteturn15file0turn15file1
- `INT` — целые числа (1, 100, -25)
- `FLOAT`, `DOUBLE` — числа с плавающей точкой (пример: 5.23E+05)
- `DECIMAL/NUMERIC` — **точные** значения с фиксированной точкой (10.99, 1000.50)  
  👉 для денег/точных расчётов чаще используют **DECIMAL**, а не FLOAT/DOUBLE

### 2.2. Строковые fileciteturn15file0turn15file1
- `CHAR(n)` — фиксированная длина
- `VARCHAR(n)` — переменная длина
- `TEXT` — большие тексты

### 2.3. Дата и время fileciteturn15file0turn15file1
- `DATE` — дата (`YYYY-MM-DD`)
- `TIME` — время (`HH:MM:SS`)
- `DATETIME` — дата + время
- `TIMESTAMP` — “мгновение” времени (обычно с учётом таймзоны/внутренней логики СУБД)

### 2.4. Булево fileciteturn15file0turn15file1
- `BOOLEAN` — `TRUE` / `FALSE`  
  Пример:
```sql
SELECT *
FROM employees
WHERE is_manager = TRUE;
```
fileciteturn15file1

---

## 3) Какие операции “естественны” для разных типов
### 3.1. Числа → арифметика и сравнения fileciteturn15file0turn15file1
```sql
SELECT quantity * unit_price AS total_price
FROM order_details;
```

```sql
SELECT (list_price / standard_cost - 1) * 100 AS markup_percentage
FROM products;
```

### 3.2. Строки → склейка, подстроки, регистр, поиск fileciteturn15file0turn15file1
```sql
SELECT CONCAT(first_name, ' ', last_name) AS full_name
FROM employees;

SELECT LEFT(product_name, 5)
FROM products;

SELECT UPPER(company)
FROM customers;

SELECT SUBSTRING(product_name, 19, 30)
FROM products;
```

---

## 4) Задания на закрепление (ключевые примеры)
### 4.1. net_price после скидки (order_details) fileciteturn15file0turn15file1
```sql
SELECT
  order_id,
  (unit_price * quantity) - (unit_price * quantity * discount) AS net_price
FROM order_details;
```

### 4.2. Полный адрес клиента (customers) fileciteturn15file0turn15file1
```sql
SELECT
  id,
  CONCAT(address, ', ', city, ', ', country_region) AS full_address
FROM customers;
```

### 4.3. Роль сотрудника через CASE (employees) fileciteturn15file0turn15file1
```sql
SELECT
  id,
  first_name,
  last_name,
  CASE
    WHEN is_manager = 1 THEN 'Manager'
    ELSE 'Employee'
  END AS role
FROM employees;
```

---

## 5) Преобразование типов (CAST) — явное
### 5.1. Что это
**Преобразование типов** — перевод значения из одного типа в другой (например, текст → число).  
В MySQL основной инструмент — **CAST**. fileciteturn15file0turn15file1

### 5.2. Синтаксис fileciteturn15file0turn15file1
```sql
CAST(expression AS target_type)
```

### 5.3. Часто используемые target_type (из урока) fileciteturn15file0turn15file1
- `SIGNED` — целое со знаком
- `UNSIGNED` — целое без знака
- `DECIMAL` / `DEC` — число с фиксированной точкой
- `CHAR` — строка
- `DATE`, `TIME`, `DATETIME`
- `BINARY`

### 5.4. Пример: текстовое количество → число (products) fileciteturn15file0turn15file1
```sql
SELECT
  product_name,
  CAST(unit_in_stock AS SIGNED) * list_price AS total_value
FROM products;
```

### 5.5. Пример: сделать “человеческий” отчёт строкой (products) fileciteturn15file0turn15file1
```sql
SELECT
  product_name,
  CONCAT(
    'Available: ', CAST(unit_in_stock AS CHAR), ' units, ',
    'Price: $', CAST(list_price AS CHAR)
  ) AS product_report
FROM products;
```

---

## 6) Скрытые (неявные) преобразования в MySQL
**Скрытые преобразования** — это когда MySQL сам меняет типы, чтобы выполнить операцию. Это удобно, но может давать неожиданные результаты, если не понимать правила. fileciteturn15file0turn15file1

### 6.1. Строка “как число” в арифметике → станет числом fileciteturn15file0turn15file1
```sql
SELECT '100' + 50 AS result;   -- 150
```

### 6.2. Число при склейке со строкой → станет строкой fileciteturn15file0turn15file1
```sql
SELECT CONCAT('The total amount is ', 150) AS message;
```

### 6.3. Сравнение числа и строки → строка станет числом fileciteturn15file0turn15file1
```sql
SELECT *
FROM products
WHERE unit_in_stock = '50';
```

### 6.4. Приоритет типов (идея)
- если смешать **целое** и **float** → целое станет float  
- при “строка + число” в CONCAT → число станет строкой  
- при арифметике “строка-число” (если строка похожа на число) → строка станет числом fileciteturn15file0turn15file1

---

## 7) Практические правила безопасности (коротко)
- Для отчётов и читаемого вывода: **CAST в CHAR + CONCAT**.
- Для вычислений: **явно CAST в SIGNED/DECIMAL**, если есть сомнения в типе.
- Не хранить числа “как текст”, если предполагаются вычисления (иначе постоянные CAST).
- При фильтрации/сравнениях старайся сравнивать **одинаковые типы** (число с числом, дата с датой).

---

## 8) Мини-шпаргалка
```sql
-- посмотреть “склеить строки”
SELECT CONCAT(a, ' ', b) AS full FROM t;

-- явное приведение
SELECT CAST('123' AS SIGNED) AS n;
SELECT CAST(10.99 AS CHAR) AS s;

-- безопаснее для вычислений
SELECT CAST(unit_in_stock AS SIGNED) * list_price AS total_value
FROM products;

-- пример скрытого преобразования (знать!)
SELECT '100' + 50 AS r;
```
